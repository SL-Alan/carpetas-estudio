$(document).ready(function(){
    //.remome
    // $('#contenedor').remove();

    // ---- Elimina todos los hijos
    // $('#contenedor').children('').remove();
    // ---- Elimina solo uno de los hijos 
    // $('#contenedor').children('#segunda').remove();
    $('#contenedor').empty();
})